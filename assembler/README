
This directory contains all the necessary code and parameters
to try and assemble an image.

What's what:

* assemble.py : script that will assemble an image
* mikhail_geom : directory containing flat text files with geometry parameters, this particular oen is mikhails geometry
* PyCSPadImage : pyana source code that actually does the assembly


